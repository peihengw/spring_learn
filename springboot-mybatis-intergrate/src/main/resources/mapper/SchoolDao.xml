<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tencent.mapper.SchoolDao">

        <select id="selectById" resultType="com.tencent.model.School">
            select id,name,site from school where id=#{id}
        </select>
</mapper>